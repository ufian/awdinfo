version: "2"

networks:
  mynetwork:
    external:
      name: web

services:
  awdinfo:
    image: ufian/telegram
    command: python /src/awdinfo.py
    volumes:
    - .:/src
    networks:
      default: {}
      mynetwork:
        aliases:
        - awdinfo